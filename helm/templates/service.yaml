apiVersion: v1
kind: Service
metadata:
  name: studytrack-app-service
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.app.port }}
      targetPort: {{ .Values.app.port }}
      nodePort: {{ .Values.service.nodePort | default 30080 }}
  selector:
    app: studytrack-app