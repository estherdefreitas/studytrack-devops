# prod values
namespace: studytrack-prod
image:
  repository: ghcr.io/estherdefreitas/studytrack-devops
  tag: "0be929e"

app:
  port: 8080
  previewPort: 8080
  replicas: 2

postgres:
  image: postgres:16-alpine
  host: studytrack-db-rw
  port: 5432
  db: studytrack-db-rw
  user: produser
  password: prodpassword
  pvcSize: 1Gi

service:
  type: NodePort
  nodePort: 30081

env:
  JAVA_OPTS: "-Xms512m -Xmx1024m"
  SPRING_PROFILES_ACTIVE: prod